// slice() es una función que devuelve un nuevo array que contiene los elementos del array original que se encuentran dentro del rango especificado.

const animals = ['cat', 'dog', 'bird', 'fish', 'snake', 'turtle']

console.log(animals.slice(2))
console.log(animals.slice(2, 4))
console.log(animals.slice(2, -1))
console.log(animals.slice())